name: gainpath_domain
description: Pure Dart business contracts shared by GainPath applications.
publish_to: none
resolution: workspace
version: 0.1.0

environment:
  sdk: '>=3.6.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
