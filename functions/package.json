{
  "name": "gainpath-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc && node -e \"require('./' + require('./package.json').main)\"",
    "test": "vitest run",
    "typecheck:tests": "tsc -p tsconfig.test.json",
    "test:emulator": "firebase emulators:exec --project demo-gainpath --config ../firebase/emulator.test.json --only firestore,storage \"vitest run --config vitest.emulator.config.ts\"",
    "serve": "pnpm run build && firebase emulators:start --only functions,firestore",
    "deploy": "pnpm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@firebase/rules-unit-testing": "^4.0.0",
    "firebase": "^11.0.0",
    "ajv": "^8.17.1",
    "firebase-tools": "^13.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
